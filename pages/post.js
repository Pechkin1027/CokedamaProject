import { sql_queary } from "../lib/db";

const handler = async (_,res) => {
    try {
        `SELECT * FROM cokedama_products
        ORDER 
        
        ` 
    }catch (e){

    }
};

export default handler; 